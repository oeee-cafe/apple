<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
<plist version="1.0">
<dict>
	<!-- The iOS OAuth client id signing in with Google uses (GoogleSignIn.swift):
	     Google Cloud console > APIs & Services > Credentials > iOS, in the same
	     project as the site's web client, and listed in the site's
	     [google].app_ids so it takes the tokens that name it. Left empty, a
	     press on the site's Google button still comes to the app -- the site
	     sends Google to the native sheet on iOS and macOS (app_sign_in.jinja in
	     oeee-cafe/web) -- and the app answers it as a sign-in that failed.
	     Not a secret, and there is no secret: the sign-in is a public client
	     with PKCE. ASWebAuthenticationSession catches Google's redirect itself,
	     so the reversed client id needs no CFBundleURLTypes entry. -->
	<key>GoogleClientID</key>
	<string>371775747741-6g94c9vfojlsosc1673ppv99b2nmacd5.apps.googleusercontent.com</string>
	<key>UILaunchScreen</key>
	<dict>
		<key>UIColorName</key>
		<string>Ground</string>
	</dict>
</dict>
</plist>
